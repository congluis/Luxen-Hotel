<tr>
    <td data-kt-accommodation-filter="accommodation_name">
        <div class="d-flex align-items-center">
            @if (!string.IsNullOrEmpty(Model.Thumbnail))
            {
                <div class="symbol symbol-50px me-5">
                    <img src="@Model.Thumbnail" alt="@Model.Name" />
                </div>
            }
            <div class="ms-5">
                <a asp-action="edit" asp-route-id="@Model.Id" class="text-gray-800 text-hover-primary fs-5 fw-bold">
                    @Model.Name
                </a>
            </div>
        </div>
    </td>
    <td class="text-center">@Model.MaxOccupancy</td>
    <td class="text-center">@(Model.Area) m²</td>
    <td class="text-center">@Model.Price</td>
    <td class="text-center">N/A</td>
    <td class="text-center">
        @if (Model.Status == LuxenHotel.Models.Entities.Booking.Accommodation.AccommodationStatus.Published)
        {
            <span class="badge badge-light-success">Published</span>
        }
        else if (Model.Status == LuxenHotel.Models.Entities.Booking.Accommodation.AccommodationStatus.Unpublished)
        {
            <span class="badge badge-light-danger">Unpublished</span>
        }
        else if (Model.Status == LuxenHotel.Models.Entities.Booking.Accommodation.AccommodationStatus.MaintenanceMode)
        {
            <span class="badge badge-light-warning">Maintenance</span>
        }
        else if (Model.Status == LuxenHotel.Models.Entities.Booking.Accommodation.AccommodationStatus.FullyBooked)
        {
            <span class="badge badge-light-secondary">Fully Booked</span>
        }
    </td>
    <td class="text-center">
        <a class="btn btn-sm btn-light btn-flex btn-center btn-active-light-primary" data-kt-menu-trigger="click"
            data-kt-menu-placement="bottom-end">
            <span>Select</span> <i class="fa-regular fa-circle-right fs-5 ms-4 ms-1"></i>
        </a>
        <div class="menu menu-sub menu-sub-dropdown menu-column menu-rounded menu-gray-600 menu-state-bg-light-primary fw-semibold fs-7 w-125px py-4"
            data-kt-menu="true">
            <div class="menu-item px-3">
                <a asp-action="edit" asp-route-id="@Model.Id" class="menu-link px-3">Edit</a>
            </div>
            <div class="menu-item px-3">
                <a href="/accommodations/@Model.Id" target="_blank" class="menu-link px-3">Preview</a>
            </div>
        </div>
    </td>
</tr>
